# CXSearch 开发进度记录

日期：2026-04-22

## 今天完成的内容

- 已确认前端原型设计方向：
  - 首页采用 `A 左主列放大`
  - 顶部主导航保留 `首页 / 我的站点 / 推荐发现`
  - 右侧保留 `搜索 / 新增站点 / 主题切换 / 齿轮设置 / 账号`
  - `齿轮设置` 进入统一管理中心
  - `简洁 / 详情模式` 不放顶部全局导航，只在内容浏览页按需出现

- 已生成设计与计划文档：
  - `docs/superpowers/specs/2026-04-22-cxsearch-frontend-prototype-design.md`
  - `docs/superpowers/plans/2026-04-22-cxsearch-frontend-prototype.md`

- 已在 `prototype/` 下完成第一阶段前端原型骨架：
  - Vite + Vue 3 + TypeScript 项目初始化
  - 顶部导航壳完成
  - 首页基础原型完成
  - 管理中心基础壳完成
  - 管理中心侧边导航完成

## 当前已存在的关键文件

- 项目配置：
  - `prototype/package.json`
  - `prototype/tsconfig.json`
  - `prototype/vite.config.ts`

- 应用入口：
  - `prototype/src/main.ts`
  - `prototype/src/App.vue`
  - `prototype/src/router/index.ts`

- 共享组件：
  - `prototype/src/components/AppTopNav.vue`
  - `prototype/src/components/ThemeToggle.vue`
  - `prototype/src/components/SearchBar.vue`
  - `prototype/src/components/CardTile.vue`
  - `prototype/src/components/SectionHeader.vue`

- 页面：
  - `prototype/src/pages/HomePage.vue`
  - `prototype/src/pages/ManagementPage.vue`

- 管理中心组件：
  - `prototype/src/components/management/ManagementSidebar.vue`

- 测试：
  - `prototype/src/components/__tests__/AppTopNav.test.ts`
  - `prototype/src/pages/__tests__/HomePage.test.ts`
  - `prototype/src/pages/__tests__/ManagementPage.test.ts`
  - `prototype/src/components/management/__tests__/ManagementSidebar.test.ts`

## 今天踩过的坑

- 当前目录不是 git 仓库，所以没有 commit。
- 运行 `vite` / `vitest` / `build` 时，沙箱会拦 `esbuild` 的子进程，后续继续开发时需要用放开权限的方式跑验证命令。
- 早期执行过 `tsc -b`，在 `prototype/src/` 下残留过旧的 `.js` 文件，导致测试一度导入到旧路由逻辑。
- 已删除这些旧文件：
  - `prototype/src/main.js`
  - `prototype/src/components/__tests__/AppTopNav.test.js`
  - `prototype/src/router/index.js`

## 最新验证结果

已实际跑过并通过：

```powershell
npm run test -- src/components/__tests__/AppTopNav.test.ts src/pages/__tests__/HomePage.test.ts src/pages/__tests__/ManagementPage.test.ts src/components/management/__tests__/ManagementSidebar.test.ts
npm run build
```

结果：

- 4 个测试通过
- build 通过

## 明天建议直接继续的顺序

1. 实现 `我的站点` 页
2. 实现 `推荐发现` 页
3. 实现管理中心剩余子面板：
   - 分类管理
   - 标签管理
   - 分享 / 推荐管理
   - 导入 / 导出
   - 显示与偏好
4. 实现：
   - 登录 / 注册页
   - 站点添加 / 编辑页
   - 插件弹窗页
5. 最后统一补：
   - 全局样式 token
   - 响应式细化
   - 页面级 `简洁 / 详情模式`

## 明天继续时的提示词

可以直接说：

`继续昨天的 prototype，先做我的站点页和推荐发现页`

## 2026-04-30 继续记录

- 注册 / 登录页：
  - 统一认证页输入框视觉，避免同一表单内出现多套输入框颜色。
  - 登录密码、设置密码、确认密码支持“查看 / 隐藏”切换。
- 登录态权限切分：
  - 公共页面保留：`/`、`/discover`、`/auth`、`/ui-design`。
  - 私有页面需要登录 token：`/my-sites`、`/my-sites/:siteTitle`、`/settings`、`/site-editor`、`/extension-popup`。
  - 未登录访问私有页面时跳转到 `/auth?redirect=原地址`。
  - 登录 / 注册成功后优先回到 redirect 指定页面，没有 redirect 时进入 `/settings`。
  - 顶部导航未登录时隐藏“我的站点”和“设置”，登录后展示个人入口。
- 后续优化记录：
  - 已在 `docs/CXSearch-future-ideas-backlog.md` 记录 `IDEA-002 UI 功能控件图标化`。
  - 图标替换后续由用户决定引入 icon 库或逐项指定后再统一处理，当前不提前替换。
- 验证：
  - `npm run test`：10 个测试文件、30 个测试通过。
  - `npm run build`：通过。
